/** @jsx React.DOM */
var React = require('react');

var Name = 'pageNotFound';

var PageNotFound = React.createClass({
  displayName: name,
  render: function () {
    return (
      <div>PageNotFound</div>
    );
  }
});

module.exports = {
  url : null,
  name : name,
  module : PageNotFound
};
